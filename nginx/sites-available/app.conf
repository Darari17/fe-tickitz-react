server {
        listen          80;

        location / {
                root /var/www/client;
                index index.html;
                try_files $uri $uri/ /index.html;
        }

        location /api/ {
        proxy_pass 				http://be-tickitz:8080/;
        }
}